- **No more "bad response from the server."** If Earshot ever crashed or was force-quit, its speech engine could stay behind and block the next launch until a restart. Now the engine always stops with the app, and every launch gets its own.
- **Skipping speaker detection really skips it.** Earshot no longer downloads a speaker model you didn't choose.
